---
name: brainstorming
description: "You MUST use this before any creative work - creating features, building components, adding functionality, or modifying behavior. 在实现之前先探索用户意图、需求和设计。适用于：创建新功能、构建组件、修改行为。"
metadata:
  openclaw:
    emoji: "🧠"
---

# Brainstorming Ideas Into Designs（头脑风暴转化为设计）

通过自然的协作对话帮助将想法转化为完整的设计和规格。

首先理解当前项目上下文，然后逐个提问来完善想法。一旦理解要构建什么，展示设计并获得用户批准。

<HARD-GATE>
在展示设计并获得用户批准之前，不要调用任何实现 skill、写任何代码、搭建任何项目或采取任何实现行动。这适用于每个项目，无论看起来多简单。
</HARD-GATE>

## 反模式："这太简单了不需要设计"

每个项目都要经过这个流程。待办清单、单函数工具、配置修改 —— 全部。"简单"项目恰恰是未审视的假设造成最多浪费工作的地方。设计可以很短（对于真正简单的项目几句话即可），但你必须展示并获得批准。

---

## 检查清单

必须按顺序完成这些项目：

1. **探索项目上下文** — 检查文件、文档、最近提交
2. **提供视觉伴侣**（如果话题涉及视觉问题）— 这是单独的消息，不与澄清问题组合
3. **提出澄清问题** — 一次一个，理解目的/约束/成功标准
4. **提出 2-3 种方法** — 带权衡和你推荐
5. **展示设计** — 按复杂度缩放的部分，每部分后获得用户批准
6. **编写设计文档** — 保存到 `docs/specs/YYYY-MM-DD-<topic>-design.md` 并提交
7. **过渡到实现** — 调用 writing-plans skill 创建实现计划

---

## 流程图

```
探索项目上下文 → 视觉问题? → [提供视觉伴侣] → 提出澄清问题
    → 提出 2-3 种方法 → 展示设计部分 → 用户批准?
    → [是] → 编写设计文档 → 调用 writing-plans skill
    → [否] → 返回展示设计部分（修订）
```

**终止状态是调用 writing-plans。** 不要调用 frontend-design、mcp-builder 或任何其他实现 skill。头脑风暴后唯一调用的 skill 是 writing-plans。

---

## 流程详解

### 理解想法

- 首先检查当前项目状态（文件、文档、最近提交）
- 在详细提问之前，评估范围：如果请求描述多个独立子系统（如"构建一个平台，包含聊天、文件存储、账单和分析"），立即标记。不要花问题去细化一个需要先分解的项目细节。
- 如果项目对单个规格来说太大，帮助用户分解为子项目：独立的部分是什么、如何关联、应该按什么顺序构建？然后通过正常设计流程头脑风暴第一个子项目。每个子项目有自己的规格 → 计划 → 实现周期。
- 对于适当范围的项目，一次问一个问题来完善想法
- 尽可能使用多选题，但开放式也可以
- 每条消息只问一个问题 — 如果话题需要更多探索，分解为多个问题
- 关注理解：目的、约束、成功标准

### 探索方法

- 提出 2-3 种不同方法及权衡
- 以你的推荐和推理对话式展示选项
- 首先展示推荐选项并解释原因

### 展示设计

- 一旦你相信理解要构建什么，展示设计
- 按复杂度缩放每个部分：如果直接，几句话；如果微妙，最多 200-300 字
- 每部分后问目前看起来是否正确
- 涵盖：架构、组件、数据流、错误处理、测试
- 如果有说不通的，准备回去澄清

### 隔离和清晰的设计

- 将系统分解为更小的单元，每个有明确目的，通过定义良好的接口通信，可以独立理解和测试
- 对于每个单元，你应该能回答：它做什么、如何使用、依赖什么？
- 有人能不看内部就理解单元吗？能改内部而不破坏消费者吗？如果不能，边界需要工作。
- 更小、边界良好的单元也更容易与你工作 — 你更好地推理能一次保持在上下文中的代码，文件专注时编辑更可靠。文件变大时，通常是做太多事的信号。

### 在现有代码库中工作

- 提出变更前探索当前结构。遵循现有模式。
- 现有代码有问题影响工作时（如文件太大、边界不清、责任纠缠），包含针对性改进作为设计的一部分 — 就像好开发者在工作的代码中改进一样。
- 不要提出无关重构。专注于服务当前目标。

---

## 设计完成后

### 文档

- 将验证的设计（规格）写入 `docs/specs/YYYY-MM-DD-<topic>-design.md`
  - （用户对规格位置的偏好覆盖此默认值）
- 将设计文档提交到 git

### 实现

- **【必须】调用 writing-plans skill 创建详细实现计划**
- 不要调用任何其他 skill。writing-plans 是下一步。

---

## 链式调用

```
[当前 skill: brainstorming]
           ↓
    用户批准设计？
           ↓ [是]
    编写设计文档
           ↓
    【必须】调用 writing-plans skill
           ↓
    [下一个 skill: writing-plans]
```

**完成此 skill 后唯一调用的 skill 是 writing-plans。**

**绝不允许：**

- 跳过 writing-plans 直接写代码
- 调用 frontend-design、mcp-builder 或任何实现 skill
- 不保存设计文档就结束

---

## 关键原则

- **一次一个问题** — 不要用多个问题淹没
- **优先多选题** — 尽可能比开放式更容易回答
- **YAGNI 无情** — 从所有设计移除不必要功能
- **探索替代方案** — 在确定前总是提出 2-3 种方法
- **增量验证** — 展示设计，继续前获得批准
- **保持灵活** — 有说不通的，回去澄清

---

## 视觉伴侣

基于浏览器的伴侣，用于在头脑风暴期间展示模型、图表和视觉选项。作为工具可用 — 不是模式。接受伴侣意味着它可用于受益于视觉处理的问题；不意味着每个问题都通过浏览器。

**提供伴侣：** 当你预期即将到来的问题涉及视觉内容（模型、布局、图表），同意一次：

> "我们正在处理的一些内容如果能展示给你看可能更容易解释。我可以在网页浏览器中展示模型、图表、比较和其他视觉效果。这个功能还比较新，可能比较耗费 token。想试试吗？（需要打开本地 URL）"

**此提议必须是单独的消息。** 不要与澄清问题、上下文摘要或任何其他内容组合。消息应只包含上述提议。等待用户响应再继续。如果他们拒绝，继续纯文本头脑风暴。

**每个问题的决定：** 即使在用户接受后，对每个问题决定是使用浏览器还是终端。测试：**用户看到是否比读到更好理解？**

- **使用浏览器**处理视觉内容 — 模型、线框、布局比较、架构图、并排视觉设计
- **使用终端**处理文本内容 — 需求问题、概念选择、权衡列表、A/B/C/D 文本选项、范围决策

关于 UI 话题的问题不自动是视觉问题。"在这个上下文中个性意味着什么？"是概念问题 — 使用终端。"哪个向导布局更好？"是视觉问题 — 使用浏览器。
